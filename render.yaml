services:
  - type: web
    name: country-agent
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python api/main.py
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: PORT
        value: 10000